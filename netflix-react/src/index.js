import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import App from "./App";

// ReactJS starting code which gets root element from index.html and renders the components above it!!!
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
